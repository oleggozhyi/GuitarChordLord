<Page x:Class="GuitarChordLord.ChordsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ignore="http://www.ignore.com"
      xmlns:local="using:GuitarChordLord" xmlns:Mvvm="using:GuitarChordLord.Mvvm" mc:Ignorable="d ignore"
      d:DesignHeight="768"
      d:DesignWidth="1366"
      x:Name="ChordListPage"
      DataContext="{Binding ChordList, Source={StaticResource Locator}}">
    
    <Page.Resources>
    </Page.Resources>

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock  Text="{Binding  PageTitle}"
            FontSize="56"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Left"
                   FontFamily="Segoe UI Light" 
                   Margin="120,49,0,0" />
        
        <GridView Grid.Row="1" Mvvm:GridViewItemClickCommand.Command="{Binding SelecyChord}">
            
        </GridView>
    </Grid>
</Page>
